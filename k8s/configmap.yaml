apiVersion: v1
kind: ConfigMap

metadata:
  name: laravel-config
  namespace: laravel

data:
  APP_ENV: production
  DB_HOST: mysql-service
  DB_PORT: "3306"
  DB_DATABASE: demoproject
  DB_USERNAME: laravel
